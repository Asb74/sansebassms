workflows:
  ios-build-and-publish:
    name: iOS Build and Publish to App Store
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Hello
        script: echo "iOS Build OK"

  generate-ios-artifacts:
    name: Generate iOS Artifacts for GitHub
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Hello
        script: echo "Generating Artifacts OK"
